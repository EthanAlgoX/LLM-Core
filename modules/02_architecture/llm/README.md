# LLM Pretrain 分类

本目录聚焦语言模型预训练：

- `nanoGPT`：最小可读训练实现
- `megatron`：并行训练与规模化工程

## 子模块目录说明（统一）
- `code/`：主流程脚本，展示该模块最核心训练/推理步骤。
- `data/`：示例语料、数据切分结果或数据说明文件。
- `models/`：最终导出的模型权重，面向推理与复用。
- `checkpoints/`：训练中间快照（step 级别），用于断点续训与回溯。
- `output/`：可视化结果、训练指标、实验总结与日志文件。
