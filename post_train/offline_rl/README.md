# Offline RL 分类

本目录聚焦离线强化学习：

- `cql`
- `bcq`

核心区别：
- `cql` 偏保守 Q 约束
- `bcq` 偏行为策略动作约束

## 子模块目录说明（统一）
- `code/`：算法主流程（离线数据训练 + 策略评估）。
- `data/`：离线经验数据、状态动作样本或 toy 环境配置。
- `models/`：最终策略或价值函数参数。
- `checkpoints/`：训练中间状态，便于恢复与对比。
- `output/`：指标曲线、策略结果、Q 值分析与实验总结。
