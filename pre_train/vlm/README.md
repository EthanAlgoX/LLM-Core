# VLM 分类

本目录聚焦视觉语言模型：

- `blip2`
- `llava`
- `flamingo`

可用于对比不同视觉-语言融合策略。

## 子模块目录说明（统一）
- `code/`：主流程脚本，展示图像编码、跨模态融合、文本生成等关键步骤。
- `data/`：图片样本、文本标注、路径索引或数据描述。
- `models/`：最终导出的多模态模型参数（用于推理）。
- `checkpoints/`：训练过程阶段快照（可恢复训练状态）。
- `output/`：预测结果、可视化图片、指标统计和日志。
